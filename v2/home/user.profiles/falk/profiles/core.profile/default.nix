# TODO: Move firefox into core? Maybe make firefox a submodule of `web-browser.profile` that imports it (that way if I ever decide to change my default browser I just have to change it there9.
{
   imports = [
       ./locale.nix
   ];
}
