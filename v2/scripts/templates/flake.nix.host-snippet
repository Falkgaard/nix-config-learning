# This snippet has one placeholder meant to be substituted: HOST
         HOST = lib.nixosSystem {
            modules     = [ ./hosts/HOST.host ];
            specialArgs = { inherit inputs outputs; };
         };

